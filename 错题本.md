# 错题本

> 开发这个模组时**实际踩过**的坑，按「症状 → 根因 → 以后怎么避免」记。
> 目的只有一个：同一个坑不踩第二次。
>
> 最后更新：2026-09-25

---

## 一、编译与构建

### 1. 改了依赖版本，只跑增量编译 → 游戏启动时崩

**症状**：升级 Goety 依赖后跑 `gradlew compileJava`，显示 BUILD SUCCESSFUL。
结果一启动游戏就炸：

```
java.lang.IllegalAccessError: class com.qiuyue.goetyominous.common.init.ModAttributes
  tried to access private field com.Polarice3.Goety.init.ModAttributes.ATTRIBUTES
```

**根因**：Gradle 是**增量编译**。某个源文件自己没被编辑过，它就沿用上次编译好的 class，
**不会拿新版本的依赖重新校验一遍**。依赖方把 public 字段改成 private 这种事，
编译期一声不吭，等到运行时才炸。

**以后**：只要动过 `build.gradle` 里的依赖版本，就老老实实跑
`gradlew clean compileJava`（约 3 分钟），别信增量编译的 BUILD SUCCESSFUL。

---

### 2. 日志文件写在 build/ 里，把 clean 卡死

**症状**：`gradlew clean compileJava` 报
`java.io.IOException: Unable to delete directory 'F:\java\java\build'`。

**根因**：`clean` 要删掉整个 `build/` 目录，而我正把编译日志往里写 ——
自己锁住了自己要删的文件。

**以后**：编译日志一律写到 `$env:TEMP` 下，或者任何 `build/` 之外的地方。

---

### 3. 往 run/mods 里丢发布版的 mod jar

**症状**：开发环境启动时报 `NoSuchMethodError` / `NoClassDefFoundError`。

**根因**：开发环境（userdev）和发布版（production）的 jar 是两种东西，
前者要经过 `fg.deobf` 解混淆，不能直接丢进 `run/mods` 当普通 mod 加载。

**以后**：联动 mod 一律用 `fg.deobf(...)` 声明进 `build.gradle` 的依赖，不要手动丢 jar。

---

### 19. git 自动合并「假成功」→ 两边各加了同一个符号 → 重复定义

**症状**：`git merge` 只报了 11 个冲突文件，全部解决后编译却失败：

```
错误: 已在 OfItems 中定义了变量 VOLT_FOCUS
错误: 已在 SpellConfig 中定义了变量 VoltSoulCost
```

而这些文件**根本没出现在冲突列表里**。

**根因**：两个分支**各自独立地**往同一个类的不同位置追加了同名成员
（比如都在自己那份里加了一个 `VOLT_FOCUS`）。git 的自动合并是按**文本块**比的，
两处改动离得远、上下文不重叠，它就认为「互不干扰」，把两份都留下了 ——
**这是假成功：文本上不冲突，语义上冲突。** 只有编译器能发现它。

**以后**：合并后**必须全量编译**，不能只看「冲突解完了」就当没事。
编译报「已定义」时，别急着删一处，先查清**哪一份是当前真正在用的**：

```powershell
# 找出符号出现的所有位置
$l = [System.IO.File]::ReadAllLines($路径)
for ($i=0;$i -lt $l.Count;$i++) { if ($l[$i] -match '符号名') { "{0}| {1}" -f ($i+1), $l[$i] } }
```

**⚠️ 只删声明是不够的。** 如果两边各自还写了一整套赋值 / 注册
（比如出现两个 `BUILDER.push("Volt")`），必须**成套成对地删**，
否则配置文件里会冒出两个同名段落。
确认哪套在用，最可靠的办法是**顺着调用方反查**：谁 `.get()` 了这个配置、谁 new 了这个类。

---

### 22. git 自动合并的另一面：一边删了、一边没动 → **静默删掉你依赖的方法**

> 这是第 19 条的**镜像**，而且更阴 —— 19 是「两边都加」，编译会报「已定义」；
> 22 是「一边删」，编译报的是「找不到符号」，而**报错的文件根本不在冲突列表里**。

**症状**：`git merge origin/master` 只报了 5 个冲突文件，全部解决后全量编译失败：

```
PossessedPaladinServant.java:2992: 错误: 找不到符号
  符号: 方法 sendAdvancedHotBarMessage(String,ChatFormatting,float)
（同类错误共 22 个，全在同一个文件里）
```

而 `PossessedPaladinServant.java` **根本没出现在冲突列表里**，
`git diff --stat HEAD -- <该文件>` 输出是**空的**（合并压根没碰它）。

**根因**：缺的这 4 个方法（`sendAdvancedHotBarMessage` / `advancedDash` /
`calculatedDashToPositon` / `applyStackingEffect`）**不在这个文件里，在它的爷爷类里**
（`PossessedPaladinServant extends IAnimatedBossServant extends IAnimatedMonsterServant`）。

协作者那一侧的 `IAnimatedMonsterServant` 把这一批 LM 移植工具方法**当成死代码删了**
（434 行 → 353 行）—— 他那边没有圣骑仆从，在他眼里确实没人调用。
而我们这边**从没碰过那个文件**，对 git 来说「一边改了、一边没动」**不算分歧**，
于是**直接采用删掉的那版，一个冲突都不报**。

**以后**：

1. 合并后**必须全量编译** —— 和 19 同一条规矩。冲突列表清零 ≠ 合并成功。
2. 报「找不到符号」但**报错文件没被合并改过**时，**别去改那个文件**，缺的东西在**父类 / 接口**里。
   按这个顺序反查：

   ```powershell
   # ① 确认报错文件本身有没有被合并动过（空 = 没动，问题不在它）
   & $g -C F:\java\java diff --stat HEAD -- <报错文件>

   # ② 看它继承自谁
   Select-String -Path <报错文件> -Pattern '^\s*public\s+class'

   # ③ 父类三方逐个 grep 缺的那个符号（:1=共同祖先 :2=我们 :3=对方）
   foreach ($s in 1,2,3) { & $g -C F:\java\java show ":$s`:<父类路径>" | Select-String '缺失的方法名' }

   # ④ 想确认这个符号在历史上哪个提交起就没了，用 pickaxe
   & $g -C F:\java\java log -S '方法名' --oneline -- <路径>
   ```

   判断口诀：**哪一方的行数变了、符号没了，就是那一方删的**。
3. **修的时候放进自己的文件，不要放回共享文件。** 放回 `IAnimatedMonsterServant`
   等于跟协作者对着干，下次合并会**同样静默地**再删一次。
   这里是把 4 个方法搬进 `IAnimatedBossServant` —— 协作者那边**没有这个文件**，
   以后合并不可能再被连带删掉。
4. 搬的时候**方法体逐字照抄**，别顺手「优化」（比如把 `else if (!(A && B))` 改成 `else`）。
   合并修复阶段只做搬运，不做重构，否则出了问题分不清是合并的锅还是重构的锅。

**顺带一提**：`IAnimatedMonsterServant` 里被删的还有 `applyEffectTo` / `sendBasicHotBarMessage` /
`yaw()` / `pitch()` / `repelEntities` / `calculatedDashToScaledVec`。
这几个**本仆从确实没用到**（编译器一个错都没报），所以**没有搬**。
以后要用到它们时，记得它们已经不在爷爷类里了。

---

### 20. `gradlew clean` 砸掉 fg_cache → 报出几千个**假**错误

**症状**：合并后跑 `gradlew clean compileJava`，冒出 **2762 个错误**，
满屏 `警告: 未知的枚举常量 Dist.CLIENT` / `Bus.MOD`，像是整个项目全废了。
错误数量大得不正常。

**根因**：`clean` 删的是整个 `build/`，**里面包含 `build/fg_cache`** ——
那是 ForgeGradle 生成的「打过补丁的 Forge 核心 jar」。删掉后它要重新生成，
而这一步失败了：

```
java.io.IOException: Failed to delete existing output file:
  build\fg_cache\...\forge-1.20.1-47.3.22-binpatched.jar
```

这个 jar 是**整个编译的依赖基础**。它一坏，所有 Forge / Minecraft 类都找不到，
于是每个引用它们的文件都报错 —— **2762 个错误全是这一件事的连带伤害**，
我们自己的代码一行没错。

**以后**：

1. **认特征**：错误数突然上千、且大量是「未知的枚举常量 `Dist.CLIENT`」这类
   **Forge 自己的东西找不到** → 别去读那些错误，直接怀疑依赖环境坏了。
2. **看日志开头，别只看结尾。** 真正的根因（`Failed to delete existing output file`）
   在**第 14 行**，而错误摘要在最后几十行。
3. **要全量重编时别用 `clean`** —— 手工删编译产物，保住 `fg_cache`：

```powershell
cmd /c ".\gradlew.bat --stop"           # 先停守护进程，否则它攥着文件不放
Remove-Item -Recurse -Force F:\java\java\build\classes
Remove-Item -Recurse -Force F:\java\java\build\tmp\compileJava
cmd /c ".\gradlew.bat compileJava"
```

（和第 2 条同源，都是 `clean` 要删的东西出了问题：
那条是「日志锁住 clean」，这条是「clean 拆掉了自己盖房子的地基」。）

---

### 24. 凭记忆写原版 API → 1.20.1 的 `PlayerAdvancements` 连 `getProgress` 都没有

**症状**：写「主人有没有拿到某个成就」的判定，很自然地写成：

```java
PlayerAdvancements progress = owner.getAdvancements().getProgress(advancement);
return progress != null && progress.isDone();
```

编译报 `找不到符号: 方法 getProgress(Advancement)`。
心想「那就是没有 getProgress，用 isDone 吧」，改成 `isDone(advancement)` —— **又报同样的错**。

**根因**：1.20.1 的 `net.minecraft.server.PlayerAdvancements` **只有**一个读法：
`getOrStartProgress(Advancement)`。`getProgress` 和 `isDone(Advancement)` 都是**后来版本才加的**。
凭印象写 API，连猜两次都猜错。

**以后**：只要冒出「这个方法应该存在啊」的念头，**别接着猜第三次**，
直接去官方映射的源码 jar 里翻。路径：

```
C:\Users\Administrator\.gradle\caches\forge_gradle\minecraft_user_repo\
  net\minecraftforge\forge\1.20.1-47.3.22_mapped_official_1.20.1\
  forge-1.20.1-47.3.22_mapped_official_1.20.1-sources.jar
```

解压出那个 `.java` 看一眼，30 秒的事：

```powershell
Add-Type -AssemblyName System.IO.Compression.FileSystem
$jar = "上面那个 jar 的完整路径"
$zip = [System.IO.Compression.ZipFile]::OpenRead($jar)
$e = $zip.Entries | Where-Object { $_.FullName -eq "net/minecraft/server/PlayerAdvancements.java" }
[System.IO.Compression.ZipFileExtensions]::ExtractToFile($e, "F:\java\java\temp_decompile\PlayerAdvancements.java", $true)
$zip.Dispose()
```

⚠️ 两个细节：
- 这个源码 jar 里**缩进是 3 个空格**（不是 4 个）。用 Grep 找 `^    public` 会**一个都匹配不到**，
  会误以为「这文件是空的」。搜 `^   public `（3 空格）才对。
- 同一个缓存目录下还有 `47.4.10`、`parchment` 等好几个版本，**认准 `mapped_official` 那个**。

---

## 二、Goety 仆从框架

### 4. 借别人的注册表 → 人家一改就崩

**症状**：同上面第 1 条那个 `IllegalAccessError`。

**根因**：原先往 `com.Polarice3.Goety.init.ModAttributes.ATTRIBUTES` 里塞我们自己的属性。
那是 Goety 的**内部字段**，2.5.57.3 把它从 public 改成 private，我们这边就崩了。
借别人的注册表用，等于把身家押在别人的私有实现上。

**以后**：自己建 `DeferredRegister`，只用对方的**公开 API**。

---

### 5. 可选联动 mod 的内容注册进了主注册表 → 别人启动就崩

**症状**：没装某个联动 mod 的整合包，一启动就崩。

**根因**：把联动内容（物品 / 实体等）注册进了共用的 `ModItems`。
没装那个 mod 的玩家，类加载时找不到依赖的类。

**以后**：联动内容必须注册进**各自的** `XxItems`，和主注册表隔离。

---

### 6. FollowOwnerGoal 构造时把导航器抄死了

**症状**：仆从召唤出来不跟随、移速慢得离谱。

**根因**：`FollowOwnerGoal` 的构造函数里直接抓了当时的导航器实例存成字段，
而 Goety 仆从会在之后替换导航器 —— 存下来的是个已经被丢弃的旧对象。

**以后**：涉及导航器的 goal，用 `entity.getNavigation()` 现取，不要在构造时抄死。

---

### 7. goal 优先级设得不够高，被跟随目标抢走

**症状**：攻击 goal 写好了却几乎不触发。

**根因**：Goety 的 `FollowOwnerGoal` 优先级是 **5**。
同优先级的 goal 按注册顺序轮询，先返回 `true` 的会锁住 MOVE / LOOK / JUMP 三个 Flag，
后面的根本轮不到。攻击 goal 优先级必须 **> 5**。

---

### 8. 配置文件的默认值改不动

**症状**：改了代码里的配置默认值，进游戏还是旧值。

**根因**：`run/config` 下的 toml 文件已经存在，**已存在的键**会覆盖代码默认值。

**以后**：改已存在的配置键，代码和 toml **两边都要改**；
或者让玩家删掉 toml 重新生成。

---

### 18. 新加的行为 goal 优先级 0 → 把近战全抢走了

**症状**：给圣骑补了一条「后跃拉开距离」的 goal（贴脸就往后跃，好让两招远程起手能触发）。
结果用户一进游戏就说「感觉近战变少了好多」。招式一个没删，近战却几乎看不见了。

**根因**：**优先级决定的是「谁先被问」，不只是「谁抢得过谁」。**
那条 goal 为了压过所有攻击 goal（优先级 1），注册在了**优先级 0** ——
于是每 6 秒冷却一到就**必触发**（没加掷骰），一次占掉 28 tick 的后空翻，
跃开之后又必然接一个远程招。算总账：每轮循环真正留给近战的只有一两秒，
剩下的时间全被后跃和远程招吃掉。单看那条 goal 本身没错，错在**没人算过它一次占多久**。

**以后**：给仆从加新的「行为」goal（不是新招式，是改变节奏的那种）之前，先算一笔账：

```
它一次占用多久 × 触发频率 → 剩下的时间还够不够主力招式用？
```

优先级 0 尤其危险 —— 排在所有攻击前面，等于把主力招式的出手机会按顺序让给它。
拿不准就先放优先级 1（和攻击同级），跑一跑再决定要不要往前提。

**⚠️ 这个坑和第 7 条是一体两面**：第 7 条是优先级**太低**被打不过，
这条是优先级**太高**把别人全挤没了。两个方向都要防。

（这条 goal 已按要求整体回退，相关记忆见 `paladin-retreat-backflip`。）

---

## 三、LM（传奇怪物）移植

### 9. 没覆写 isAlliedTo → 范围招把自己的主人打了

**症状**：仆从放范围招，主人和同伴一起掉血。

**根因**：原版是 BOSS，`isAlliedTo` 只管同族；仆从必须再加上「主人 / 同伴」这一层。

---

### 10. 招式概率找不到 —— 它藏在 registerGoals 的匿名子类里

**症状**：想调某个招的触发概率，翻遍实体类找不到它定义在哪。

**根因**：概率不是字段，是注册 goal 时**匿名子类里写死的**
（`random.nextFloat() * 100 < JumpRandom` 这种写法）。
在方法签名里看不到，要去**注册现场**看。

---

### 11. 渲染器要读的字段，忘了放在客户端也会执行的那半边

**症状**：动画 / 特效在客户端完全不出现，或者数值永远是 0。

**根因**：`UpdateWithAttack` 是在 `aiStep` 里调的，客户端也会跑；
但如果某个字段只在服务端 tick 里赋值，客户端就永远读到默认值。

**以后**：渲染器要读的东西，赋值必须放在**客户端也会执行到**的路径上。

---

### 12. 只注册了 goal，忘了搬 UpdateWithAttack → 「动作照播，一刀打不到」

**症状**：招式动画播得好好的，但完全没伤害。

**根因**：伤害判定写在 `UpdateWithAttack` 的状态分支里，和 goal 是**两个地方**。
只注册 goal = 只有动画，没有判定。

**⚠️ 这个坑踩了两次**（状态 8、状态 31），移植新招式时特别要对照检查。

---

### 15. 「要求最小距离」的招式，移植成仆从后永远不触发

**症状**：扑跃剑阵（招 22，要求目标 **≥ 6 格**）和翻滚式二连震地（招 12，要求 **≥ 5 格**）
几乎从来不放。概率对、冷却对、注册顺序对，就是不放。
（同一个根因还波及**招 38 掷三叉戟旋转**，它要求 ≥ 7 格，只不过是二阶段专属，不容易注意到。）

**根因**：**这个距离在原版是「玩家造成的」，不是招式自带的。**
原版圣骑是 BOSS，玩家会边打边跑，5~16 格这个空档随时都有，
招式只要「等目标进到这个带子里」就行。
移植成仆从之后，圣骑跟着主人、敌人也不跑，它一路平推贴着脸打 ——
而**所有近战招要的都是 5~7 格以内**，于是距离永远停在 0~3 格，
那三招下限条件（`distanceTo(target) > 5 / > 6 / >= 7`）恒为 `false`。

**以后**：移植任何带「最小距离」的招式之前，先问一句：
**「这个距离在原版是谁造成的？」**

| 答案 | 仆从身上会怎样 | 要做什么 |
|---|---|---|
| 玩家跑出来的 | 距离永远不出现，招式永不触发 | 得自己补位移（试过一次「后跃拉开距离」，结果踩了新坑，见第 18 条） |
| 招式自己跳出来的 | 正常 | 什么都不用做 |

反过来也成立：只要看到某招 `canUse()` 里有 `> 某个数`，就要警惕它不是为仆从设计的。

---

### 16. 限伤写在 actuallyHurt 里 = 形同虚设

**症状**：配了「单次最多 21 点」的限伤，实际挨打该掉多少还是掉多少，
尤其配了护甲之后，完全看不出上限存在。

**根因**：`actuallyHurt` 拿到的 `amount` **已经过完护甲和抗性**了。
一记 100 点的伤害走进来：先被动态减伤砍成 50 → 再过护甲变成 15 →
最后才 `min(15, 21)`，还是 15。**上限 21 根本没碰到。**
「限在最后一步更准」这个直觉是错的 —— 越靠下游数字越小，上限越没机会生效。

**以后**：**「上限」这类限制要放在伤害链的最上游**（`hurt()` 开头、护甲之前），
「比例减免」放中间，「护甲」放最后。
LM 原版 `IAnimatedBoss.hurt()` 就是把 `Math.min(damageCap, amount)` 写在第一行的。

---

### 21. 移动速度是「基础速 × 导航倍率」，调跟随/闲逛要覆写 getFollowSpeed()

**症状**：圣骑仆从**追人打人**的速度和敌对版一模一样，一点问题没有；
但**平时跟着主人走、没事闲逛**时明显偏快 —— 四百血的重甲 BOSS 在你旁边小跑，观感很急躁。

**根因**：移动速度**不是一个数，是两个数相乘**。决定性的那一行在原版 `MoveControl` 里：

```java
// MoveControl.java:98
this.mob.setSpeed((float)(this.speedModifier * this.mob.getAttributeValue(Attributes.MOVEMENT_SPEED)));
```

也就是 `实际速度 = 导航倍率 × MOVEMENT_SPEED 属性`。

LM 原版写的是「**小基础速 × 大倍率**」：`0.1 × 3.0 = 0.30`，
那个 3.0 写在 `IMoveGoal(this, false, 3.0F)` 的注册处。
移植时我们改成了「大基础速 × 小倍率」：`0.3 × 1.0 = 0.30`。
**乘积一样，所以打架时完全看不出问题** —— 这就是它能藏这么久的原因。

问题出在**没经过那个 3.0 的地方**，两处都直接吃基础属性：

- Goety 的 `FollowOwnerGoal` —— 倍率取自 `IServant.getFollowSpeed()`，**默认 1.0**
- Goety 的 `WanderGoal` —— 倍率是注册时我们自己传的，当时传的也是 `1.0D`

于是跟随 / 闲逛都跑到了 0.30。而原版是 BOSS，**根本没有这两个 goal**
（它只有 `LookAtPlayerGoal` / `RandomLookAroundGoal`，那俩**只转头、不走路**），
所以这个偏差在原版永远暴露不出来。仆从多了这两个 goal，代价才显形。

**怎么查**：凡是怀疑某只仆从移速不对，就**把「属性值」和「IMoveGoal 的倍率」一起抄下来相乘**，
再跟同一个包里别的同类仆从横着比。这次就是这么发现圣骑和蔓生巨像是仅有的两个异类：

| 仆从 | IMoveGoal 倍率 | 基础移速 | 追击速度 | 跟随速度 |
|---|---|---|---|---|
| 飓旋仆从 | 3.0 | 0.1 | 0.30 ← 照原版 | 0.30（覆写了 `getFollowSpeed()=3.0`） |
| 云铸魔像仆从 | 3.0 | 0.1 | 0.30 ← 照原版 | 0.30（覆写了 `getFollowSpeed()=3.0`） |
| **圣骑仆从 / 蔓生巨像仆从** | **1.0** | **0.3** | 0.30 | 0.30 ← 没覆写。**最终决定：就这样**，见下面的📌 |

**以后**：

1. 移植 LM 这类「基础速特小 + 倍率特大」的怪，**两个数照抄，别自作主张合并**。
   觉得基础速 0.1「小得不像话」想把它调大 —— **那正是这个坑的入口**。
2. 真要调「跟随 / 闲逛」的观感，**覆写 `IServant.getFollowSpeed()`**，
   再配 `WanderGoal` 的倍率。**别动基础移速，也别动 IMoveGoal 的倍率**
   —— 那两个数一动，追击速度就跟着变，会把调了很久的战斗手感搞乱。

   ```java
   // 同包的飓旋 / 云铸魔像就是靠这个覆写救回来的（它们基础速 0.1，不覆写慢得没法玩）
   @Override
   public double getFollowSpeed() {
       return 3.0D;   // 3.0 × 基础移速 0.1 = 0.30
   }
   ```

   > 📌 **但这条路我们在圣骑身上走了一遍，又退回来了（2026-09-25）—— 记牢结论，别再走第二遍。**
   >
   > 圣骑仆从先后试过：跟随 `0.5` → `0.9`，闲逛 `0.5` → `0.75`。
   > **两次实机手感都不对，现在圣骑和蔓生巨像一样：三档（追击 / 跟随 / 闲逛）全是 0.30，
   > 一个都不覆写、WanderGoal 倍率也写回 1.0D。**
   >
   > 所以上面第 2 条是「**技术上的可能性**」，不是「推荐做法」。
   >
   > ⚠️ **真要调，先记住这条判据：跟随速度的下限是「明显快过玩家疾跑」，不是「快过玩家走路」。**
   > 玩家自身 MOVEMENT_SPEED 也是 0.1，**疾跑再 ×1.3 = 0.13**；玩家赶路是一直按着疾跑键的。
   > 再扣掉拐弯 / 绕障碍 / 被怪挤开的寻路损耗，只比 0.13 高一点点（比如 0.15）
   > 实机就是「跟不上、过于慢」。要调的话目标至少 0.25 往上。

   `getFollowSpeed()` 是 `IServant` 上的 `public default` 方法（默认返回 `1.0`），
   经 `Summoned.followGoal()` 虚调用，所以覆写就能生效。

   > ⚠️ 仓库里那两个 Goety 源码目录是**别的 MC 版本**
   > （`Goety-master` = 1.16.5、`Goety-3-main` = 1.21.1），**别信它们**。
   > 查 1.20.1 的 API 得用 `javap` 反编译真实依赖 jar —— 这次差点照着错版本改了方向。

3. 顺带一条定心丸：Goety 的 `FollowOwnerGoal` 在拉开 **16 格**时会**自动传送**过来，
   所以**压低跟随速度不会**重演第 57 号任务那种「不跟随、跟丢了」的问题。

---

### 23. 改了 `entityData` 的**默认值** → 读档 / 生成路径被这个默认值悄悄接管

**症状**：圣骑的沉睡系统做完，仪式召唤出来确实是躺着的。但只要**存档退出再进来**，
它会当场自己站起来演一遍苏醒动画，喂东西那一步被整个跳过。

**根因**：`AWAKENED` 这个布尔，原版默认 `false`（=「还没醒」），
我们为了兜底把它改成了 `true`（理由见下）。于是：

```
读档 → readAdditionalSaveData 里 setSleep(true) → 状态号 = 34（躺下）✅
     但 AWAKENED 取的是「默认值」true → 状态 34 那条 goal 的闸门是开的
     → 下一 tick 就收尾 → 直接切 35 开始苏醒演出 ❌
```

**关键是**：`AWAKENED` **不进存档**（原版和我们都不存），所以读档时它永远是**默认值**。
默认值一改，所有「靠默认值兜底」的路径（读档、`finalizeSpawn`、`/summon`）
语义就全跟着变了 —— 而且**不报错、不崩溃**，只是行为反了。

**以后**：只要动了 `entityData.define(X, 默认值)` 里那个默认值，**必须回头把三条路径都过一遍**：
1. `readAdditionalSaveData`（读档）
2. `finalizeSpawn`（各种生成方式）
3. 刚 `new` 出来还没初始化的那一瞬间

哪条路径依赖「取默认值就是对的」，就得在那里**显式写死**一次。
本次的修法就是两处补 `this.setAwakened(false);` —— 读档发现人在睡、仪式召唤时。

> 顺带记一下当初为什么把默认值改成 `true`：如果哪天冒出个没走 `finalizeSpawn` 的生成路径，
> 醒来状态（`true`）比沉睡状态（`false`）安全得多 —— 沉睡的圣骑是**免伤 + 不动 + 不锁敌**的，
> 掉进那个状态又没人喂，就是个玩家删不掉的砖头。兜底方向没错，错的是改完没回头查路径。

---

### 25. 语言键前缀写反 → 不崩、不报错，只是**什么都不显示**

**症状**：给圣骑写了 22 个新台词键，编译过一次，游戏也**正常启动、日志干净**。
唯一的问题是：动作栏里**一个字都不出现**。

**根因**：语言键的段序写反了。

| | |
|---|---|
| 语言文件里实际是 | `message.goetyominous.possessed_paladin_servant.awaken.1` |
| 代码里写成了 | `goetyominous.message.possessed_paladin_servant.awaken.1` |

本模组的约定是 **`message.goetyominous.…`**（看现成的 `message.goetyominous.teletor_switched_tool`），
不是 `goetyominous.message.…`。两段前后一颠倒，22 个键**全部**是空的。

**为什么危险**：`Component.translatable(key)` 查不到键时**不会抛异常**，只是显示不出来。
于是「键写错了」和「台词时机还没到」在游戏里**长得一模一样** —— 都表现为「什么都没有」。
更要命的是旧键 `legendary_monsters.message.possessed_paladin_*` 在 LM 的 jar 里**依然存在**，
所以**就算我们整个语言文件都没加、或者漏改了某一处，游戏也不会给任何提示**。

**以后**：语言键这种「字符串对字符串」的改动，**代码和语言文件必须当成一次改动一起验**。
改完立刻跑下面这个自查（**已实测可用**），最后一行打印出来的就是漏掉的键，**正常情况什么都不打印**：

```powershell
# 1) 把 java 里引用的所有语言键抓出来
$keys = Get-ChildItem F:\java\java\src\main\java -Recurse -Filter *.java |
        Select-String -Pattern '(message\.goetyominous|goetyominous\.message)\.[a-zA-Z0-9_.]+' -AllMatches |
        ForEach-Object { $_.Matches.Value } | Sort-Object -Unique

# 2) 和语言文件对一遍
$lang = Get-Content F:\java\java\src\main\resources\assets\goetyominous\lang\zh_cn.json -Encoding UTF8 -Raw
$missing = @()
foreach ($k in $keys) { if (-not $lang.Contains($k)) { $missing += $k } }

Write-Output ("java 引用键=" + $keys.Count + "  语言文件缺失=" + $missing.Count)
$missing
```

⚠️ 这个脚本第一次跑出来是「**22 个键全部缺失**」。当时的第一个反应是「脚本有 bug」，
差点转头去改脚本 —— 其实它是对的，键**真的**全错了。

**自查脚本报「全部都错」的时候，先怀疑代码，别先怀疑脚本。**
（当时顺手拿短串 `"awaken.1"` 去试，它恰好能命中，更加坐实了「脚本坏了」的错觉 ——
要验也得拿**整键**去验。）

⚠️ 两个写法上的注意：
- 抓键的正则要把**两种段序都写进 alternation**（`(message\.goetyominous|goetyominous\.message)`），
  否则前缀写反的那些**恰恰会被漏掉** —— 那就白查了。
- `Select-String` 后面接 `ForEach-Object { $_.Matches.Value }`，`Matches` 是集合、会自动展开，
  不用再套一层循环。

---

### 30. 别人明明有公开 API，我又抄了一份一模一样的

**症状**：做「弹射物去 LM 化」时，把命中后叠的「灵魂碎裂」效果也当成弹射物自己的东西搬了 ——
新建了效果类 `SoulEater`、注册表 `LmEffects`、工具类 `LmEntityUtil`，还提取了图标、加了两条语言。
结果同一次命中会挂上**两个** `soul_fracture`。

**根因**：把「**这发弹射物独有的东西**」和「**全模组共用的东西**」混为一谈了。

`ModEffects.SOUL_FRACTURE` 是传奇怪物的**公开 API**，用它是完全正常的。
它一共 4 处在用：

| 谁 | 在哪 |
|---|---|
| 幻影匕首 | `ThrownPhantomDagger.onHitEntity` |
| 圣骑本体（剑砍中） | `PossessedPaladinServant` 3 处 |
| 灵魂三叉戟 | LM 的 `SoulTridentEntity.onHitEntity` 里**写死**的 |

自己再注册一个 `goetyominous:soul_fracture`，两个效果**各减各的血上限**：
0.8 × 0.8 = **0.64**，而不是 0.8。图标也会并排显示两个一模一样的。

**以后**：动手搬一个资源（状态 / 伤害类型 / 音效 / 粒子 / 属性）之前，
**先 grep 它的使用处数量**：

- 只有这发弹射物自己在用 → 可以搬，改注册名就行；
- **别处也在用**（尤其是**对方代码里写死的**）→ 直接用对方的，别拆成两份。

⚠️ 和第 4 条不矛盾，两条说的**是相反方向的两种错**：

- 第 4 条：**别去碰别人的私有东西**（往 Goety 的私有字段塞注册表）→ 自己建注册表；
- 第 30 条：**别人有公开 API 就别再抄一份** → 直接调。

分界线是「**对方的公开 API 够不够用**」：够用就直接用，不够用才自己建。
前提是那个 mod 是**硬依赖**（LM 就在 `build.gradle` 里，`implementation` + curse.maven）。

**「去 LM 化」到底该做哪些**：只做**弹射物自己的**东西 —— 飞行基类、伤害类型、数学换算、
动画计时器、拖尾粒子。「共用的效果」不在其列。

（和第 27 条是同一类毛病的两种表现：**都是「动手前少花了两分钟去查」**。）

---

## 四、工具与流程

### 13. Grep 的输出会把 `//` 显示成 `\ `

**症状**：用 Grep 带上下文看代码时，看到一行以 `\ ` 开头、`<\b>` 结尾，
看起来像语法错误，差点动手去改。

**根因**：Grep 工具在某些行上渲染有偏差，**文件本身不是那样**。

**以后**：只要怀疑某行是语法错误，**直接用 Read 读那一行确认**，
不要凭 Grep 的输出下结论。（这个坑踩了两次。）

---

### 14. 倒放原版动画当过渡 —— 效果不好

**症状**：给圣骑做的「躺下沉睡」演出，实机看着很别扭。

**根因**：倒放拼出来的过渡，起点是原版末帧、终点强行对齐到另一段动画的第 0 帧，
而原版自己这两帧就差 7.5 度（右腿），接缝骗不过眼睛。

**以后**：别再主动提议「倒放某段原版动画当过渡」这个套路。

---

### 17. Edit 报「找不到字符串」时，用 PowerShell 看真实字符，别靠肉眼数缩进

**症状**：明明是从 Read 输出里原样抄下来的一段 javadoc，Edit 却报
`String to replace not found in file`。反复核对好几遍都觉得一模一样。

**根因**：那一行的真实缩进是 **5 个空格**，抄的时候只写了 1 个。
javadoc 的行（` * ...`）在 Read 的输出里和真实空格数对不上，肉眼数不出来。

**以后**：Edit 连续失败两次就别再猜了，直接 dump 字符码看真相：

```powershell
$p = '文件的完整路径'
$lines = [System.IO.File]::ReadAllLines($p)
$lines[128].ToCharArray() | ForEach-Object { '{0:X4}' -f [int]$_ }
```

一眼就能看出 `0020 0020 0020 0020 0020 002A` = **5 个空格 + `*`**。
（和上面第 13 条是同一类问题：工具显示出来的东西，不能当准。）

---

### 26. 中文 Windows 上编译错误日志是 GBK，搜 `error` 一条都搜不到

**症状**：编译失败后：

```powershell
Select-String -Path build_log.txt -Pattern "error|FAILED"
```

只捞到几条 deprecation warning 和 `FAILED`，**真正的 error 一条都没有** ——
看上去像是「没有错误但构建失败」。

**根因**：javac 在中文 Windows 上输出的是 **GBK 编码**，而日志被按 UTF-8 读，
「错误:」三个字变成乱码 `����`。而且 javac 的错误关键字本来就是**中文的「错误」**，不是 `error`。

**以后**：读编译日志一律加 `-Encoding Default`（= 系统 ANSI 代码页），并且抓中文关键字：

```powershell
# 抓某个文件相关的错误
Get-Content build_log.txt -Encoding Default |
    Select-String -Pattern "PossessedPaladinServant.java" | ForEach-Object { $_.Line }

# 抓错误明细 —— 「符号:」「位置:」这两行才是根因
Get-Content build_log.txt -Encoding Default |
    Select-String -Pattern "符号:|位置:" | ForEach-Object { $_.Line }
```

⚠️ javac 的「找不到符号」是**三行**：

```
文件:行号: 错误: 找不到符号
  符号:   方法 getProgress(Advancement)
  位置: 类 PlayerAdvancements
```

**光看第一行没用**（只知道哪一行出错，不知道为什么），「符号:」那行才告诉你编译器到底在找什么。
（第 24 条就是这么查出来的。）

---

### 27. 移植一个「别人自建的基类」之前，先和原版同名类逐字对比

**症状**：准备花 200 行把传奇怪物的 `AbstractFlyingProjectile` 抄成本地版。
抄之前顺手解出原版的 `AbstractHurtingProjectile` 对比了一下 —— 发现**逐字一致**，
对方只是改了类名、再动了 4 处（`shouldBurn` 改成 false、删掉拖尾烟、
删掉入水气泡、删掉已无人调用的 `getTrailParticle`）。

**根因**：很多 mod 的「自建基类」其实是**原版类的复制粘贴改名**。

**以后**：动手抄之前，先花两分钟做「同名类对比」。分两种情况：

- 差异**用手指头数得完**，而且都集中在几个**独立方法**上
  → 直接继承原版 + 覆写那几个方法，一行都不用抄；
- 差异藏在某个大方法的**中间**（比如夹在 `tick()` 里）
  → 那才需要抄一份。这次正是这种：3 处差异里 2 处在 `tick()` 中间，
    覆写 `tick()` 等于把整个方法重抄一遍，所以最后还是抄了整个类。

**别省这两分钟** —— 这次差点白抄 200 行。

---

### 28. 自定义伤害类型有「看不见的另一半」：`data/minecraft/tags/damage_type/`

**症状**：给弹射物建了个自己的伤害类型 `goetyominous:servant_ghost`，
json 写了、代码里 `getHolderOrThrow` 也拿到了 ——
但打出来的伤害**悄悄变低**，没有任何报错。

**根因**：伤害类型 json 只管「死亡消息怎么写、难度怎么缩放」。
**「会不会被护甲挡住」是写在 tag 里的**：

```
data/minecraft/tags/damage_type/bypasses_armor.json
```

原作的 `legendary_monsters:ghost` 就列在那个文件里（所以它无视护甲）。
只搬 json、不搬 tag，伤害就会被护甲吃掉一截 —— 而且**什么都不报**。

**以后**：搬伤害类型时，**顺手去对方 jar 里搜一遍 tag**：

```powershell
$zip.Entries | Where-Object { $_.FullName -like "*tags/damage_type*" }
```

新伤害类型该出现在哪些 tag 里，一个都不能漏。
（这属于「静默失效」家族 —— 和语言键前缀写反、注册名写错是同一类。）

---

### 29. 直接从 mod 的 jar 反编译，方法名是 SRG 混淆名，抄不了

**症状**：拿 Vineflower 直接反编译传奇怪物的 jar，得到的是

```java
public void m_8119_() { ... }
this.m_7678_(pX, pY, pZ, ...);
```

满屏 `m_xxxxx_`，根本没法照着写代码。

**根因**：发布用的 jar 里是 **SRG 名**（Forge 的混淆名），
而我们写代码时用的是**官方映射名**（`tick` / `moveTo`）—— 两套名字对不上。

**以后**：要「抄代码」时，去解 **ForgeGradle 的官方映射 jar**：

```
C:\Users\Administrator\.gradle\caches\forge_gradle\minecraft_user_repo\
    net\minecraftforge\forge\1.20.1-47.3.22_mapped_official_1.20.1\
    forge-1.20.1-47.3.22_mapped_official_1.20.1-sources.jar
```

里面已经是官方映射名，能直接抄。
（这也是第 24 条「凭记忆写原版 API」的正解：**去这个 jar 里读真源码**，而不是猜。）

⚠️ 它是**反编译产物**，所以方法上没有 `@Override` 注解 ——
别以为「没写 @Override 就不是覆写」。
